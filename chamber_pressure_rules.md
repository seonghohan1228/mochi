# 스윙 컴프레서 챔버 압력 결정 규칙 (He 2020 기반)

**목적**: 이미 구현된 `V_s(θ)`, `V_c(θ)` 를 입력으로 받아 `p_s(θ)`, `p_c(θ)` 를 결정하고, MBD의 가스력 `F_suc`, `F_dis` 로 넘긴다.

**표기 규칙**

| 태그 | 의미 |
|---|---|
| `[He]` | He et al. (2020) IJR 112, 125–135 에서 확인된 처리 — 그대로 따름 |
| `[확장]` | He가 하지 않은 처리 — 우리가 추가. 근거 명시 |
| `[He-미해결]` | He가 명시적으로 범위 밖이라고 밝힌 부분 |
| `[확인필요]` | 원논문에서 재확인할 것 |

> **원칙**: 이 문서의 규칙은 **기하에 무관**하다. 구조가 바뀌면 `V(θ)` 와 §2의 특성각만 바뀌고, §4~§6의 압력 규칙은 한 줄도 바뀌지 않는다.

---

## 1. 입출력 계약 및 시간축

### R1.1 — 입력 계약

구현체는 다음만 요구한다.

| 입력 | 요구사항 |
|---|---|
| `V_s(θ)`, `V_c(θ)` | θ ∈ [0, 2π), 연속, 단위 명시 (m³ 권장) |
| `dV_s/dθ`, `dV_c/dθ` | **반드시 해석적 미분 또는 스플라인의 해석 미분** |
| 특성각 4개 | §2 |
| 유체 물성 | `p_s`, `p_d`, `T_s`, 냉매 (R410A/R32 등) |

> **R1.1.a [금지]** — 체적 테이블에 유한차분을 적용해 `dV/dθ` 를 만들지 말 것. 노이즈가 압력에 그대로 증폭되어 실린다.

### R1.2 — 두 시간축 (버그 1순위)

**입자(parcel) 관점** `[He]` — He는 사이클을 **0 ~ 4π (두 바퀴)** 로 서술한다. 한 가스 덩어리가 0~2π 에 흡입되고, **다음** 바퀴 2π~4π 에 압축·토출된다.

**순간(instantaneous) 관점** — MBD가 요구하는 것. 임의의 θ 에서 흡입 챔버와 압축 챔버가 **동시에 존재**한다.

### R1.3 — 변환 규칙 (필수)

입자 위상 τ ∈ [0, 4π) 에 대해 체적을 다음으로 정의한다.

```
V_parcel(τ) = V_s(τ)          ,  τ ∈ [0, 2π)     # 흡입 중
V_parcel(τ) = V_c(τ - 2π)     ,  τ ∈ [2π, 4π)    # 압축·토출 중
```

**적분은 τ = 0 → 4π 로 한 챔버만** 수행한다. 두 챔버를 동시에 관리하지 말 것.

### R1.4 — 출력 접기 규칙

적분이 끝나면 `[0, 2π)` 로 사상한다.

```
p_s(θ) = p_parcel(θ)
p_c(θ) = p_parcel(θ + 2π)
```

---

## 2. 특성각 — 도면에서 읽어야 할 숫자

### R2.1 — 기하가 정하는 4개 `[He]`

| # | 기호 | 사건 | 출처 |
|---|---|---|---|
| ① | `φ` | 흡입 포트 **개방** (챔버가 흡입 포트에 연결) | `[He]` θ=φ 에서 흡입 시작 |
| ② | `2π` | 흡입 **종료**, 체적 최대 | `[He]` θ=2π 에서 체적 최대 |
| ③ | `θ_dp` | 토출 포트 **노출 시작** | 구조 의존 — 도면에서 측정 |
| ④ | `4π − δ` | 토출 포트 **차단** → 밀폐 체적 형성 | `[He]` |

`δ ≡ 2π − θ_dp,inst` (순간 관점의 토출 포트 각)

> **참고 초기값** (Brancher & Deschamps 2014, 롤링피스톤): 흡입 포트 중심 27°, 유효 개방 14°~40°. 자릿수 감으로만 사용.

### R2.2 — 압력이 정하는 1개 (기하 아님)

| ⑤ | `θ_vo` | 토출 **밸브 개방** | `p_c ≥ p_d + F_spring,0/(C_Af·A_port)` |

> **R2.2.a** — `θ_vo` 를 상수로 하드코딩하지 말 것. **반드시 압력 조건으로 판정**한다. 운전조건이 바뀌면 이동한다.

### R2.3 — seal-over 병합 반각 `[확장]`

```
α_merge = asin( (w_vane/2) / R_cyl )
```

병합 구간: `θ ∈ [0, α_merge] ∪ [2π − α_merge, 2π)` (순간 관점)

---

## 3. 상태 변수

### R3.1 — 레벨별 상태 변수

| 레벨 | 상태 변수 | 비고 |
|---|---|---|
| **0** | 없음 (대수적) | 폴리트로픽 관계식만 |
| **1** | `(m, T)` 또는 `(m, u)` per CV | + 밸브 `(x, ẋ)` |
| **2** | 위 + 사장체적 CV `(m, T)` | + 누설 |

### R3.2 — 압력 결정 `[확장]`

**레벨 1 이상에서는 `p` 를 직접 적분하지 말 것.** 상태방정식에서 유도한다.

```
p = EOS(ρ = m/V, T)          # CoolProp 등 실기체
```

> **근거**: `p` 를 적분하면 질량보존이 깨진다. 누설·역류가 들어오는 순간 물리적으로 무의미해진다.

---

## 4. 위상별 압력 규칙 — 흡입측

### R4.1 — 위상 (a): 흡입 밀봉 재팽창, τ ∈ [0, φ) `[He]`

**조건**: 새 챔버가 형성되었으나 어느 포트와도 연결되지 않음 → **밀폐 체적**

`[He]` 잔류 가스가 흡입압까지 팽창한다.

**레벨 0 규칙**

```
p(τ) = p_0 · ( V_parcel(0⁺) / V_parcel(τ) )^n
p(τ) = max( p(τ), p_s )          # 흡입압 도달 시 클램프
```

- `p_0` = 직전 사이클 종료 시 잔류 압력 (≈ `p_d` 근방). **R7.2의 주기 수렴으로 결정**하고 하드코딩 금지.
- `V_parcel(0⁺)` = 클리어런스 체적 (R5.2)

> **R4.1.a [주의]** — 이 구간은 짧지만 압력이 `p_d` → `p_s` 로 급락한다. 생략 금지.

### R4.2 — 위상 (b): 흡입, τ ∈ [φ, 2π) `[He]`

```
p(τ) = p_s          (상수)
```

**`[He]` 근거 (원문)**: 스윙 컴프레서에는 **흡입 밸브가 없으므로** 흡입 위상의 가스 압력 손실을 무시한다.

> **R4.2.a [권장]** — 레벨 0/1 에서 이 규칙을 유지할 것. 정밀화 유혹을 견딜 것. 근거는 R4.3.

### R4.3 — 흡입 정밀화 금지 규칙 `[확장]`

**`C_Av,s` 를 상수로 두는 lumped 흡입 모델을 만들지 말 것.**

**근거** (Brancher & Deschamps 2014, 실측 대비):

| | 실측 | CFD 기반 가변 `C_Av` | **고정 `C_Av` (0.3/0.7)** |
|---|---|---|---|
| 흡입 손실 | 2.2 W | 1.9 W | **13.4 W** (6배 과대) |
| 토출 손실 | 18.2 W | 16.7 W | **4.1 W** (4배 과소) |

→ **어설픈 상수 계수보다 He식 `p = p_s` 상수가 낫다.**

정밀화하려면 `C_Av,s(θ)` 를 CFD로 구해야 하며, 이는 레벨 2 이상의 작업이다.

> **투자수익 판단**: 흡입측 압력 변동은 수십 kPa. 토출측은 ~20 bar 를 휩쓴다. `F_suc`, `F_dis` 목적에서 흡입 정밀화는 2차 효과다.

---

## 5. 위상별 압력 규칙 — 압축·토출측

### R5.1 — 위상 (c): 압축, τ ∈ [2π, τ_vo) `[He]`

```
p(τ) = p_s · ( V_parcel(2π) / V_parcel(τ) )^n
```

**폴리트로픽 지수 `n` 규칙**

| 레벨 | 처리 |
|---|---|
| 0 | `n = κ` (adiabatic) `[He]` `[확인필요]` |
| 0+ | `n` = polytropic 상수 (1.1~1.2), 오일·열전달·누설 반영 |
| 1 | **`n` 을 쓰지 말고 에너지식 직접 적분** `[확장]` |

**레벨 1 에너지식** `[확장]`

```
d(m·u)/dτ = − p · dV/dτ  +  Σ ḣ·(dm/dτ)  +  Q̇_wall/ω
```

> **R5.1.a [경고]** — `n` 은 결과를 크게 흔든다. 문헌에서 같은 압축기에 adiabatic / polytropic / isothermal 을 적용하면 토출압이 **9.3 / 6.8 / 4.9 bar** 로 갈린다. `n` 을 튜닝 노브로 남기면 방어 불가. 레벨 1에서는 **결과로 나오게** 할 것.
>
> **R5.1.b [필수]** — 레벨 0 에서도 **`n` 민감도 스윕(κ, 1.15, 1.0)** 을 반드시 수행. 이것이 모델 불확실도의 상한이다.

### R5.2 — 클리어런스 체적 바닥 규칙 `[확장]` **(필수)**

```
V_eff(τ) = max( V_parcel(τ), V_clearance )
```

> **R5.2.a [경고]** — `V → 0` 이면 `p V^n = const` 에서 **`p → ∞` 로 발산**한다. `V_clearance` (토출 리세스 + 베인/부시 클리어런스) 를 반드시 바닥으로 넣거나, 누설(R6.3)을 넣을 것. **둘 중 하나는 필수.**
>
> 압축 슬리버의 피크 압력은 **기하가 아니라 누설과 클리어런스가 정한다.**

### R5.3 — 위상 (d): 토출

**개방 조건**

```
if p_c ≥ p_d + F_spring,0 / (C_Af · A_port):   밸브 개방
```

**레벨 0** `[He]`

```
p(τ) = p_d          (즉시 개방, 밸브 관성 무시)
```

**레벨 1 — 밸브 동역학** `[확장]` **(사용자 목표의 핵심)**

```
m_eq·ẍ + c·ẋ + k·x = (p_c − p_d) · C_Af(x, θ) · A_port
ṁ_out = C_Av,f(x, θ) · A_port · (등엔트로피 노즐, 초크 판정 포함)
```

두 계수의 물리:

- **`C_Av,f(x, θ)`** — 밸브 열림 **과** 롤러 근접도 둘 다의 함수. 밸브가 많이 열릴수록 롤러에 의한 유동 제한이 더 크게 작용.
- **`C_Af(x, θ)`** — 열림이 작을 때 유속 증가로 밸브에 걸리는 압력하중이 **감소**.

> **R5.3.a [핵심]** — **과압축(over-compression) 스파이크가 부시 하중의 피크를 만든다.** `p_c` 가 `p_d` 를 순간적으로 초과하는 구간의 크랭크각을 **반드시 기록**할 것. 이것이 윤활 결합 시 최소유막두께 위치와 겹치는지가 다음 단계의 핵심 질문이다.

### R5.4 — 위상 (e): 혼합(mixing) `[He-미해결]`

**He 원문**: 토출·혼합 위상의 압력 변화는 가스 혼합 과정에만 관련되는데 **매우 복잡하여 이 연구의 범위를 벗어난다.**

**그런데 He 자신의 결론**: 토출·혼합 위상에서 스윙로드에 걸리는 힘이 **크게 증가**하며, 이 구간이 큰 힘과 마모로 성능을 제약한다.

> **⚠ He 모델의 가장 약한 고리가, 하필 부시 하중이 가장 큰 구간이다. 여기가 연구 기여 지점.**

**물리**: 접촉점이 `θ_dp` 를 지나면 **토출 리세스 사장체적이 압축 챔버에서 흡입 챔버로 넘어간다.** 소량의 고압 가스가 역류하여 흡입 챔버 압력을 상승시킨다 `[He]`.

**레벨 0 규칙**: `p = p_d` 유지 (He와 동일)

**레벨 1 규칙 — bracket** `[확장]` **(권장)**

두 극한을 **모두** 돌려 감싼다.

```
상한: 사장체적 연결 없음 (고압 유지)  →  블레이드 Δp 최대
하한: 즉시 평형 (동일 압력)          →  블레이드 Δp 최소
```

부시 하중 피크가 두 극한 사이에서 몇 % 차이 나는지로 판정. 차이가 작으면 확정하고 넘어간다.

**레벨 2 규칙 — 스로틀 2체적** `[확장]`

판정 기준:

```
Π = τ_평형 / Δt_창 = V_dead·ω / (C_d · A_eff(θ) · c · Δθ)
```

| Π | 처리 |
|---|---|
| < 0.1 | 동일 압력 OK |
| 0.1 ~ 1 | 경계 → bracket |
| > 1 | **반드시 ODE** |

> **R5.4.a [경고]** — `p_d/p_s ≈ 2.93 > 임계 압력비 1.77` 이므로 블로다운은 **초크 상태로 시작**한다. "즉시 평형"은 시작부터 물리적으로 불가.
>
> **R5.4.b [경고]** — 전이 구간의 `A_eff` 는 포트 전체 면적이 아니다. 접촉점이 포트를 쓸며 **0에서 열린다**. 즉 창의 앞부분에서 `Π → ∞`. "동일 압력"은 정확히 반대인 `A_eff → ∞` 를 가정한다.
>
> **R5.4.c** — 운전주파수가 높을수록 `Π` 가 커져 가정이 나빠진다. 30~90 Hz 인버터면 고주파에서 재검토.

### R5.5 — 위상 (f): 토출 밀폐 체적, τ ∈ [4π − δ, 4π) `[He]`

토출 포트가 차단되어 밀폐 체적 형성. 잔류 고압 가스가 **다음 사이클의 R4.1 재팽창으로 넘어간다** → 루프 닫힘.

```
p(τ) = p(4π−δ) · ( V_parcel(4π−δ) / V_eff(τ) )^n
p_0 (다음 사이클) = p(4π)
```

> **R5.5.a** — 구조에 `δ` 가 존재하는지 도면에서 확인. 밸브 없는 설계 연구들이 "토출 밀폐 체적 없음"을 장점으로 내세우는 것이 이 구간 제거를 뜻한다.

---

## 6. 예외 처리 규칙

### R6.1 — seal-over 병합 `[확장]`

**조건**: `θ ∈ [0, α_merge] ∪ [2π − α_merge, 2π)` — 접촉점이 베인 폭 안에 숨어 벽 2가 소멸.

**규칙: 단일 검사체적으로 병합한다.**

**근거 (정량)** — 접촉점 근방 gap ~ `e·Δφ²/2` 에서:

```
슬리버 체적    A ≈ e·R·θ³/6         (3차)
베인 노출길이  ℓ ≈ (e·θ²/2)·(R/r)   (2차)
```

`α_merge = 5.96°`, `w = 8 mm`, `R = 38.5 mm` 기준:

| 관점 | 값 | 판정 |
|---|---|---|
| 슬리버 체적 / 행정체적 | **3.2×10⁻⁵** | 무시 |
| 슬리버 질량 / 챔버 질량 | **~8×10⁻⁵** | 무시 |
| 베인 노출 길이 | **0.018 ~ 0.037 mm** | 무시 |
| **최악 차압(16.8 bar)에서 가스력** | **0.9 ~ 1.9 N** | **kN 대비 무시** |

> **R6.1.a** — **힘 목적에서 이 구간의 압력 처리는 자유다.** 밀어낼 면적이 없으므로 압력이 무의미하다.
>
> **R6.1.b [비대칭 주의]** — `±α_merge` 는 대칭이 아니다.
> - `θ ∈ [0, +α]`: IN 슬리버(~0) + OUT(초승달 전체, **`p_s`**) → **둘 다 `p_s`, 완전 무해**
> - `θ ∈ [2π−α, 2π)`: IN(초승달 전체, `p_s`) + OUT 슬리버(**`p_d`**, ~0) → 고압 유입이나 질량 8×10⁻⁵ 로 무해
>
> **R6.1.c [선물]** — 병합이 `V → 0` 특이점을 자동 정규화한다 (R5.2 보완). 다만 **물리가 아니라 정규화임을 인지**할 것. 실제로는 밀봉이 실린더 벽에서 베인 팁으로 **이관(hand-over)** 되는 것이지 소멸하는 것이 아니다.

### R6.2 — seal-over 와 사장체적을 혼동 금지 `[확장]` **(중요)**

**세 가지를 각각 다른 스위치로 다룰 것.**

| 대상 | 크기 | 병합? | 이유 |
|---|---|---|---|
| **seal-over 슬리버** | 3×10⁻⁵ 행정 | **예** | 질량·면적 모두 무시 가능 |
| **토출 리세스 사장체적** | **0.1~0.5 cc** (슬리버의 **100~400배**) | **아니오** | Δp_s 4~13%, 고온 가스 |
| **반경방향 클리어런스 누설** | 면적 0.5~0.94 mm² | **아니오** | 초크 누설, 체적효율 |

> **R6.2.a [경고]** — 이 셋을 하나의 플래그로 묶지 말 것. seal-over 병합은 무해하지만 사장체적 병합은 무해하지 않다.

### R6.3 — 누설 `[확장]`

**최소 요구**: 압축 슬리버 발산 방지용 (R5.2 대안)

**레벨 2**: 경로별 스로틀. 우선순위는 문헌 기준 —

| 순위 | 경로 | 기여 |
|---|---|---|
| 1 | 로터 단면 ↔ 실린더 헤드 벽 클리어런스 | **51.5%** |
| 2 | 반경방향 클리어런스 (접촉점 근방) | 2위 |

`gap ≈ e·Δφ²/2` → 베인 가장자리에서 **16~31 μm**, 누설면적 **0.5~0.94 mm²**, 초크.

### R6.4 — 엔탈피 규칙 `[확장]`

> **R6.4.a [필수]** — 혼합/역류를 병합으로 처리하더라도 **에너지식은 살릴 것.** 질량만 섞고 온도를 무시하면 **흡입 가스 가열(suction gas heating)** 손실을 놓친다.

**근거**: 넘어오는 것은 ~80°C 고온 가스다. 압력 상승(수십 kPa)보다 **밀도 저하를 통한 체적효율·냉방능력 손실이 크다.** Yamamoto et al. (2014)이 흡입 채널에 **단열 공간**을 제안한 이유.

---

## 7. 수치 규칙

### R7.1 — 솔버

| 항목 | 규칙 |
|---|---|
| 적분기 | **implicit/BDF** (`solve_ivp` 의 `Radau` 또는 `BDF`) |
| 이유 | 밸브 개방 순간이 거의 불연속 → explicit 발산 |
| 포트 개폐 / 밸브 개방 | **event 함수로 정확히 포착**. 상태 스위치를 `if` 로만 처리 금지 |
| 물성 | CoolProp (R410A/R32) |

### R7.2 — 주기 수렴 (필수)

초기 잔류 상태(R4.1 의 `p_0`)를 모르므로 **steady-periodic 까지 반복**한다.

```
반복: y(4π) = y(0) 가 될 때까지
  - 단순 반복: 보통 5~20 사이클
  - Newton on residual: 수 회
수렴 판정: |y(4π) − y(0)| / |y(0)| < 1e-6
```

> **R7.2.a [금지]** — `p_0` 를 하드코딩하지 말 것. 반드시 수렴 결과로 얻을 것.

---

## 8. MBD 인터페이스

### R8.1 — 가스력

```
F_blade(θ) = [ p_c(θ) − p_s(θ) ] · H · ℓ_exposed(θ)
```

### R8.2 — 모멘트 (필수)

> **R8.2.a** — 힘뿐 아니라 **부시 중심 B 에 대한 모멘트도 함께 출력**할 것.

**근거**: 부시는 블레이드에 대해 병진(`h₀`)과 회전(`β`) **2자유도**를 가진다. 미지수가 둘이므로 **힘 평형만으로 풀리지 않고 모멘트 평형이 필요**하다. He가 가이드에 힘**과 모멘트**를 함께 세운 것, JSRAE 2008 이 유막두께**와 경사각**을 함께 측정한 것이 모두 같은 이유.

### R8.3 — 저장 필수 출력 (윤활 결합 단계의 입력)

| 변수 | 용도 |
|---|---|
| `W(θ)` 법선 하중 | Reynolds 해의 하중 평형 조건 |
| `M(θ)` 모멘트 | `β` 결정 |
| `U_rel(θ)` | 쐐기 항 |
| `θ` where `U_rel = 0` | **스퀴즈 단독 구간 = 최대 위험** |
| **과압축 피크 크랭크각** | 최소유막두께 위치와의 중첩 검사 |

---

## 9. 검증 규칙

### R9.1 — 닫힘 검사 (필수)

```
p(4π) == p(0)          # 주기성
m(4π) == m(0)          # 질량보존
```

### R9.2 — p–θ 선도 대조

문헌 형태와 비교. **Pan & Pan (2026)** 이 스윙 컴프레서 p–θ 선도를 제시하고 검증:
- 조건: `p_s` = 0.867 MPa, `p_d` = 2.542 MPa, 30~90 Hz
- 오차: 냉방능력 1%, 소비전력 5%, COP 4% 이내

### R9.3 — 지시일

```
W_i = ∮ p dV
```
adiabatic 이론일보다 크고 물리적으로 타당한 범위인지 확인.

### R9.4 — `n` 민감도 (필수)

adiabatic / polytropic / isothermal 세 가지로 하중 피크가 얼마나 흔들리는지 = **모델 불확실도의 상한**.

### R9.5 — 축동력 검증의 함정 `[확장]`

> **R9.5.a [경고]** — He 는 축동력 실측으로 4% 이내 일치를 보였다. 그러나 **부시 손실은 총 손실의 일부**이며, He 자신이 편심 베어링·스윙로드–부시·로터–실린더·스러스트 4개 항목이 지배적이라고 밝혔다.
>
> **→ 축동력 총합 일치는 부시 유막 모델의 타당성을 보증하지 못한다.** 항목 간 오차 상쇄가 가능하다. 항목별(부시 단독) 검증이 본 연구의 차별점.

---

## 10. 레벨별 구현 순서

| 레벨 | 흡입 | 압축 | 토출 | 혼합 | 노력 | 얻는 것 |
|---|---|---|---|---|---|---|
| **0** | `p_s` 상수 + 재팽창 | 폴리트로픽 `n` 고정 | 즉시 개방 | `p_d` 유지 | 1일 | p–θ 형태, 하중 자릿수 |
| **1** | 동일 | 에너지식 + CoolProp | **밸브 동역학 + 과압축** | **bracket** | 1주 | **부시 하중 피크** ← 목표 |
| **2** | lumped + `C_Av(θ)` | + 벽면 열전달 | + 누설 | 스로틀 2체적 | 수개월 | 체적효율, COP |

> **권고**: **레벨 1까지 갈 것. 레벨 2의 흡입 정밀화는 하지 말 것.** `F_suc`, `F_dis` → 부시 하중 → 기계효율 목적에서 투자수익이 명확히 레벨 1에 몰려 있다 (R4.3, R5.3.a).

---

## 부록 A. He 대비 이탈 목록 (논문 작성용)

| 항목 | He | 본 구현 | 근거 |
|---|---|---|---|
| 흡입 압력 | `p_s` 상수 | **동일** | 흡입 밸브 없음 `[He]` |
| 압축 | adiabatic `[확인필요]` | 에너지식 직접 적분 | `n` 민감도 (R5.1.a) |
| 토출 밸브 | 즉시 개방 | **동역학 + 과압축** | 부시 하중 피크 |
| 혼합 | **미해결** `[He-미해결]` | **bracket → 스로틀** | **본 연구 기여 지점** |
| seal-over | 미언급 | 병합 (정량 근거) | R6.1 |
| 누설 | — | 경로별 스로틀 | 슬리버 발산 방지 |
| 검증 | 축동력 총합 4% | **항목별 (부시 단독)** | R9.5.a |

## 부록 B. 파라미터 표 (도면/실험에서 채울 것)

| 기호 | 의미 | 값 | 출처 |
|---|---|---|---|
| `φ` | 흡입 포트 개방각 | 10.4° | 공급 (2026-07-20). 베인 폭에서 유도 안 됨 — **출처 확인 필요** |
| `β` | 압축 시작각 | 27.7° | 공급 (2026-07-20) |
| `γ` | 토출 포트 각폭 | 7.2° | 공급 (2026-07-20). 호 4.84 mm — 타당 |
| `θ_dp` | 토출 포트 노출각 | 339.6° | `2π − δ − γ` |
| `δ` | 토출 밀폐 구간 | 13.2° | 공급 (2026-07-20) |
| `w_vane` | 베인 폭 | 8 mm | 주어짐 |
| `R_cyl` | 실린더 반경 | 38.5 mm | 보어 77 mm |
| `α_merge` | 병합 반각 | 5.96° | `asin(4/38.5)`. `φ`와 **별개 값** (R6.2.a) |
| `e` | 편심 | 4.5 mm | 주어짐 = `(77−68)/2` |
| `H` | 실린더 높이 | 21 mm | 주어짐 |
| `V_clearance` | 클리어런스 체적 | **0.193 cm³** | **유도값** — 실제 형상 적분 (PHYSICS.md 3.4) |
| `V_dead` | 토출 리세스 사장체적 | 해당 없음 | 사장체적의 99%가 로터 마우스 공동 |
| `m_eq, c, k` | 밸브 등가 파라미터 | ? | 실험/문헌 |
| `p_s, p_d, T_s` | 운전 조건 | ? | 시험조건 |

## 부록 C. 참고문헌

- He, Z., Yang, X., Li, D., Wu, W. (2020). Dynamic characteristics of a swing compressor for an air conditioning system at different discharge pressures. *Int. J. Refrigeration*, 112, 125–135.
- Brancher, R.D., Deschamps, C.J. (2014). Modeling of Rolling-Piston Compressors with Special Attention to the Suction and Discharge Processes. *22nd Int. Compressor Engineering Conference at Purdue*.
- Ooi, K.T., Wong, T.N. (1997). A computer simulation of a rotary compressor for household refrigerators. *Applied Thermal Engineering*, 17(1), 65–78.
- Pan, X., Pan, S. (2026). Investigation of compression characteristics and structural optimization of the swing compressor with two discharge channels.
- Yamamoto et al. (2014). 흡입 채널 단열, 토출 포트 최적화.
- Bell, I. et al. PDSim — 오픈소스 용적형 압축기 시뮬레이션 프레임워크. `github.com/ibell/pdsim` (롤링피스톤 예제 포함; `V(θ)` 와 포트 로직만 교체하면 열역학·밸브·누설 재사용 가능)
